spring.application.name=Service_Registry
server.port=8761
eureka.instance.host=localhost
# we don't want to fetch registry as well
eureka.client.fetch-registry=false
# we don't want to register itself
eureka.client.register-with-eureka=false

